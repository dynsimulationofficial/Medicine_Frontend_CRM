services:
  medicine-frontend:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "127.0.0.1:8016:8016"

    environment:
      NODE_ENV: production
      HOSTNAME: 0.0.0.0
      PORT: 8016

      NEXT_PUBLIC_API_URL: "https://medicine-crm-backend-staging.dynsimulation.com/api/v1/managelead"
      NEXT_PUBLIC_APP_ENV: productiom
      NEXT_PUBLIC_S3_BUCKET_NAME: medicine-crm-staging-bucket-2026

    restart: unless-stopped
