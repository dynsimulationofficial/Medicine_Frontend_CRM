services:
  medicine-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8016:8016"
    environment:
      NODE_ENV: production
      HOSTNAME: 0.0.0.0
      PORT: 8016
    restart: unless-stopped